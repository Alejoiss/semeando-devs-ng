export interface Owner {
    id: string;
    name: string;
    email: string;
    address?: string;
    phone?: string;
    whatsapp?: string;
    furtherInformation?: string;
    createdAt: string;
    updatedAt: string;
}
